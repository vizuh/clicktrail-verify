{
  "name": "@vizuh/clicktrail-verify",
  "version": "0.1.0",
  "description": "Local browser and source verification for consent-aware attribution handoffs.",
  "type": "module",
  "bin": { "clicktrail-verify": "./src/cli.mjs" },
  "files": ["src", "schemas", "README.md", "LICENSE"],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "node --check src/cli.mjs && node --check src/runner.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "playwright": "^1.62.1" },
  "license": "MIT",
  "author": "Vizuh OÜ",
  "repository": { "type": "git", "url": "https://github.com/vizuh/clicktrail-verify" },
  "keywords": ["attribution", "consent", "data-layer", "tracking", "playwright", "verification"]
}

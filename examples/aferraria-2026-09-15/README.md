# A Ferraria example

This is a redacted report from a read-only synthetic browser run. It contains
no cookie values, event IDs, visitor IDs, request bodies, or customer data.
The deployment/source drift finding is specific to the inspected deployment and
should not be generalized to other ClickTrail installations.
